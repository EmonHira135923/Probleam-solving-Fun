#include<bits/stdc++.h>
#define endl '\n'
#define ll long long
#include<string.h>
#include<string>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    ll M;
    string word;
    float O;
    double P;

    cin >> N >> M >> word >> O >> P;

    cout << N << endl << M << endl << word << endl << O << endl << P << endl;

    return 0;
}